@inject NavigationManager NavManager
@if (Data != null && Data.Any())
{
    <div class="all-suppliers-widget">
        <div class="expandable-container @(_isExpanded ? "expanded" : "")">
            <div class="widget-summary-row" @onclick="ToggleExpand">
                <div class="item-icon">
                    <i class="bi bi-people-fill"></i>
                </div>
                <div class="item-info">
                    <span class="info-title">تمام کارگزاران</span>
                    <span class="info-subtitle ms-2">@Data.Count کارگزار</span>
                </div>
                <div class="item-chevron">
                    <i class="bi bi-chevron-down"></i>
                </div>
            </div>

            <div class="suppliers-list-card">
                <div class="suppliers-grid">
                    @foreach (var supplier in Data)
                    {
                        <div class="supplier-tag" @onclick="() => OnSupplierClick(supplier)">
                            @supplier.Name
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
}

@code {
    [Parameter]
    public List<SupplierItem>? Data { get; set; }

    private bool _isExpanded = false;

    private void ToggleExpand()
    {
        _isExpanded = !_isExpanded;
    }

    private void OnSupplierClick(SupplierItem broker)
    {
        NavManager.NavigateTo($"/players/brokers/{broker.Id}");
    }
}
